ps -ef|grep nginx

ps 显示进程
-A 显示所有进程
-e 同-A
-f 显示UID PPID,C 与STIME栏位
grep 查找

这条命令的意思是显示与nginx有关的进程
UID PID PPID C STIME TTY TIME CMD


UID为UserId，即用户ID，用来标识每个用户的唯一标示符
PPID是父进程ID
PID是进程ID
C 为CPU资源百分比
STIME 系统启动时间
TTY 登入者的终端机位置
TIME 使用掉的 CPU 时间。
CMD 所下达的指令为何
