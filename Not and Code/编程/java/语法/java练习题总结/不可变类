不可变对象,在内存中创建一个对象后,这个对象的内容就不可以被改变,而只能被回收了,最典型的是String
String s1 = "Hello";
String s2 = "Hello";
在字符串池中开辟一个空间并存储了 "Hello" 这个字符串,s1 和 s2 都指向了这个内存空间
如果令 s1 = "Hi"; 那么字符串池会再开辟一个空间去存储"Hi",并用s1去指向它,而不会去修改"Hello"的内容

不可变对象
概念:创建后不可以改变的对象,典型的就是java的String类

优点:
1.效率:
   提高String Pool的效率,如果你知道一个对象是不可变的，那么需要拷贝这个对象的内容时，就不用复制
它的本身而只是复制它的地址，复制地址（通常一个指针的大小）需要很小的内存效率也很高。

2.安全:
    不可变对象对于多线程是安全的，因为在多线程同时进行的情况下，如果有多个对象指向同一块内存空间,那么修改
当前线程中一个对象的值,只会该变这个对象指向的内存空间,而原来所指的内存空间不会被改变,因此其它对象不会受到
影响
比如:
String s1 = "Hello";
String s2 = "Hello";
如果令 s1 = "Hi"; s2也仍为"Hello"
