1.
设一字符串为s
若要直接查找某字符串test中有无字串s时,以下的用法可能会出现错误
p = re.compile(s)
p.findall(test)

原因,字符串s中可能有"*" "?" "\w"等,在正则表达式中并不表示字符本身而有其它含义
所以直接匹配是可能会失败的

2.没有使用最小匹配
.*?(尽可能少地匹配字符) 和 .*(尽可能多地匹配字符) 会出现不同的效果

3.正则表达式中"."表示匹配除了换行符以外所有的字符，所以用"."匹配时要考虑换行符的影响,若要让"."匹配换行符
可用re.S模式
