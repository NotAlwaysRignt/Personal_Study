向表中插入数据
INSERT INTO 表的名字(列名a,列名b,列名c) VALUES(值1,值2,值3);
例子:
INSERT INTO employee(id, name, phone) VALUES (011354,'Tom',123456);

向表中删除数据

删除行(比较常用)
DELETE FROM 表名字 WHERE 条件;
(切记一定要考虑加 WHERE ,否则整张表就被删除了)
